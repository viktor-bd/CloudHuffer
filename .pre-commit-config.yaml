# Minimal pre-commit config to run a secret scan using detect-secrets (optional tool)
# Developers should install detect-secrets locally (pip install detect-secrets)
repos:
  - repo: https://github.com/Yelp/detect-secrets
    rev: v1.3.0
    hooks:
      - id: detect-secrets
        args: ['--baseline', '.secrets.baseline']
