name: Simple CI Pipeline
on: [push, pull_request]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v3
      - name: Set up .NET SDK
        uses: actions/setup-dotnet@v3
        with:
          # Tests target net9.0, use .NET 9 SDK in CI
          dotnet-version: '9.0.x'

      - name: Restore and build backend
        run: |
          dotnet restore
          dotnet build --no-restore --configuration Release

      - name: Run backend tests
        # Run tests (allow build by removing --no-build to avoid VSTest invocation issues)
        run: dotnet test CloudHufferApi.Tests/CloudHufferApi.Tests.csproj --verbosity normal

      - name: Set up Node
        uses: actions/setup-node@v4
        with:
          node-version: '18'

      - name: Build frontend
        run: |
          cd CloudHufferWeb
          npm ci
          npm run build -- --configuration development

      - name: Echo success
        run: echo "CI build and tests completed"
